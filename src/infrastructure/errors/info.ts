console.log("package info : Infrastructure related errors");

/**
  If the errors pertain specifically to data access, external services, or
  integration with systems (e.g., a database connection failure or an external API error),
  those should reside in the infrastructure layer. This keeps infrastructure concerns
  separate from business logic.
 */