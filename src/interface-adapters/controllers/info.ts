console.log("package info : Controllers")

/**
 The controllers are responsible for orchestrating actions but delegate the
 business logic to use cases.Handle HTTP requests, translate requests into actions,
 and map responses back to HTTP responses. They reside in the interface adapter layer.
 */
